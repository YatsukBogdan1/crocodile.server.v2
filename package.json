{
  "name": "socket-chat-example",
  "version": "0.0.1",
  "description": "my first socket.io app",
  "scripts": {
    "flow": "flow",
    "flow:check": "flow check ./src/",
    "flow:build": "flow-remove-types ./src/ -d ./lib/ --all --pretty",
    "start": "yarn run flow:build && node ./lib"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "cors": "^2.8.5",
    "express": "^4.13.3",
    "socket.io": "^2.1.1"
  },
  "devDependencies": {
    "flow": "^0.2.3",
    "flow-bin": "^0.86.0",
    "flow-remove-types": "^1.2.3"
  }
}
